import NavBar from "./components/NavBar";
import Tabs from "./components/Tabs";

function App() {
  return (
    <div>
      <NavBar />
      <Tabs />
    </div>
  );
}

export default App; // make available outside this file, returns jsx (react components)
// tells the browser what should appear on the screen
